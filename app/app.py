#!/usr/bin/env python3

# test it by navigating to the folder in terminal, call "from app import *"
import logging

logging.basicConfig(level=logging.INFO)


def new_field():
    ''' 
    Carcassonne wise, new fields depend on characteristics of adjacent 
    fields
    '''
    return "helper"



